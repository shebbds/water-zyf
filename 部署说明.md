# 工作台部署与运维说明（一揽星河挂锦帆）

卫生许可个人业务工作台：纯静态站点（`workbench/`），电脑/手机公网可访问。
本说明覆盖：**部署、Supabase 云同步、高德地图、多设备同步/删除传播、数据替换、常见问题**。

> 安全提示：以下 Supabase publishable key 与高德 Key/安全密钥已写入 `workbench/app.js`，公开仓库会暴露它们。
> Supabase **anon/publishable key 本就设计为可公开**（前端必须用它）；高德安全密钥同理。若介意，请把仓库设为私有。

---

## 1. 仓库与部署

- **GitHub**：`git@github.com:shebbds/water-zyf.git`（SSH，分支 `main`）。
  - 本机对 GitHub 的 HTTPS 推送不稳定（代理偶发 502），统一走 SSH。
- **Render**：服务名 `water-zyf`，纯静态站点，发布目录 `./workbench`，无需构建（`render.yaml` 已配好）。
- **发布流程**：改完代码 → `git push origin main` → Render 自动重部署。
- **线上地址**：`https://water-zyf.onrender.com`

---

## 2. Supabase 云同步配置

数据可选同步到 Supabase，实现多设备共享。配置写在 `workbench/app.js` 的 `DEFAULT_SETTINGS`（部署版生效），也可在网页「设置界面」临时改（只存当前浏览器）。

当前值：
- 项目 URL：`https://tchrevgamfaxjmjcqeww.supabase.co`
- publishable/anon key：`sb_publishable_WDg9H-OEl5ickKrRqQpcfQ_qVFk_oM1`
- 表名：`units`

### 首次使用须在 Supabase 后台建表 + 开权限（SQL Editor 执行）
```sql
create table if not exists units (
  license    text primary key,   -- 与代码 onConflict:"license" 对应，必须唯一
  id         text,
  name       text,
  address    text,
  valid_from text,
  valid_to   text,
  lng        double precision,
  lat        double precision,
  remark      text default '',
  device_type text default '',   -- 设备类型（2026-09-07 新增）
  contact     text default ''    -- 联系人
);

```

### ⚠️ 第二步（必做，最容易漏）：放开写入权限

建表**不等于**能写入。用 Supabase 的 **Table Editor 图形界面**建的表默认开启 RLS 且没有任何策略，
此时前端读取会返回空数组、写入报 `42501 new row violates row-level security policy`，
表现为「导入成功但保存不了、刷新就没了」且**旧版本前端不报任何错**。

请**单独**复制下面这一段执行（不要和上面建表语句混在一起，也不要漏掉）：

```sql
alter table units disable row level security;
```

> 想保留 RLS 也可以，改用下面两条（二选一即可，不要都执行）：
> ```sql
> alter table units enable row level security;
> create policy "anon_all" on units for all to anon using (true) with check (true);
> ```

### 验证是否真的通了（复制整段执行，应返回 `ok`）

```sql
select (case when exists(select 1 from units limit 1) then 'ok_read' else 'ok_read_empty' end) as read_check;
```
更直接的验证（在浏览器控制台执行，应打印 `写入成功`）：
```js
const c = supabase.createClient("https://tchrevgamfaxjmjcqeww.supabase.co","sb_publishable_WDg9H-OEl5ickKrRqQpcfQ_qVFk_oM1");
c.from("units").upsert([{license:"__probe__",id:"999",name:"t"}],{onConflict:"license"})
 .then(r=>console.log(r.error?("写入失败："+r.error.message):"写入成功"));
```
若打印 `写入失败：new row violates row-level security policy...`，说明上面的权限步骤没生效，重跑一次即可。

> 日期字段用 `text` 以兼容各种格式；坐标用 `double precision`。
> 表不存在 → 前端读写被拒；表存在但 RLS 未放开 → 读返回空、写报 42501。

### 扩展字段所需列（设备类型 / 联系人）

老表若建表时没这两列，执行（新表已在建表语句中一并给出）：

```sql
alter table units add column if not exists device_type text default '';
alter table units add column if not exists contact     text default '';
```

**不加也能正常使用**：前端会自动探测云端缺列（PostgREST 42703），跳过这些字段继续同步其余内容，
并在提示里写明「云端缺列 device_type、contact」。想让这两个字段也多设备同步，执行上面两条即可。

---

## 3. 高德地图配置（地图不显示几乎都是这里）

网页端地图用高德 JS API 2.0。Key 写在 `app.js`：`amapKey` / `amapSecurity`。

**最常见故障：地图整片空白/灰、或控制台报 `INVALID_USER_SCODE` / `USERKEY_PLAT_NOMATCH`**
→ 高德 Key 的**域名白名单**没放行当前域名。

修复（高德开放平台 → 应用管理 → 找到该 Key → 设置 → 域名白名单）：
- 加 `water-zyf.onrender.com`（只填域名，不带 `https://`）
- 本地调试顺手加 `localhost` 和 `127.0.0.1`
- Key 类型须是 **Web端(JS API)**；如设了白名单则必须包含上述域名，否则被拒。

> 迁移过服务名（water-cx-workbench → water-zyf）后，旧白名单不会自动包含新域名，所以换服务后地图必现空白，需更新白名单。

---

## 4. 多设备同步 / 删除传播（重点）

同步模型：**云端（Supabase `units` 表）为准**。

- 台账里任何操作（新增/导入/编辑/办结/删除）后**立即**与云端全量对齐（`syncNow`）：
  先 `upsert` 本地全部记录，再删掉“云端有、本地没有”的残留。**不再等 1.5 秒防抖。**
- 页面打开加载 Supabase 库后，自动从云端**合并**一次（`pullCloud({merge:true})`）。
- 合并逻辑：先合并云端记录进本地（新增/修改），再**删除“本地有、但云端已无”的记录**。
  ⇒ 在某设备删除单位 → 云端少了该记录 → 其它设备自动把自己本地那份也删掉。**删除会在设备间传播。**

### 4.1 实时同步（多设备即时可见，无需刷新）— 需执行一条 SQL

开启后，任意设备增删改，其它**已打开的页面会在 1 秒内自动更新**，状态显示在顶栏徽标
（`已连接` / `连接中…` / `未连接`）。可在「设置界面 → 多设备实时同步」开关。

**必须先在 Supabase → SQL Editor 执行一次**（把表加入 Realtime 发布，否则收不到变更事件）：

```sql
alter publication supabase_realtime add table units;
```

> 若提示表已在发布中（`relation "units" is already member of publication`），忽略即可，说明已开启。
> 想确认是否生效：打开工作台看顶栏徽标是否为绿色「实时同步：已连接」；
> 仍显示「未连接」多半就是这条 SQL 没执行，或该表的 Realtime 未在后台开启。

**已知权衡（无大碍）**：因为“云端为准”，若你在**手机离线**时新增了一条且从没推上云端，下次联网拉取会被清掉（云端没有它）。主要在电脑在线维护则碰不到。
> 若以后要多设备离线新增也必保留，可升级为“墓碑(tombstone)”方案（云端加 `deleted` 列软删）——需要再改。

---

## 5. 数据替换（换底档 Excel）

页面数据来自 `workbench/seed.js` 里的 `window.SEED_DATA`（前端内置种子）。应用把数据缓存进浏览器 `localStorage`，**仅在本地为空时才读种子**。

替换步骤：
1. 用 Excel 重新生成 `seed.js`（字段：编号/单位/经营地址/卫生许可证号/有效期始/有效期止；日期转 `YYYY-MM-DD`；lng/lat 留空）。
2. 让“旧缓存失效”：把 `app.js` 里 `var LS_DATA = "hp_workbench_data_vN";` 的版本号 +1（如 `v2`→`v3`）。旧键下的数据即被丢弃，下次打开用新种子。
3. 提交并 `git push`，Render 重部署；用户刷新即见新数据。

> 注意：源 Excel **不进版本库**（延续数据安全处理），仓库只留 `seed.js`。新数据仅 3 条为测试子集时，发送完整 Excel 重新生成即可。

---

## 6. 常见问题速查

| 现象 | 原因 | 处理 |
|---|---|---|
| 地图整片空白/灰、报红错 | 高德 Key 域名白名单未放行 `water-zyf.onrender.com` | 高德后台加白名单，强刷 |
| 地图有底图但没点位 | 这 3 条数据暂无坐标（lng/lat 空） | 地图页点「🔄 地理编码全部」 |
| 删除单位后刷新又出现 | 旧同步只 upsert 不删云端 | 已修复（见第 4 节，云端为准） |
| 电脑删了、手机还有 | 删除未传播 | 已修复（云端为准合并）；手机强刷即可 |
| 网页端名字不对 | 改名需改 `index.html` 的 `<title>` 与 `.brand` | 两处一起改，UTF-8 |
| **导入/新增后保存不了，刷新就没了** | **Supabase `units` 表 RLS 未放开，写入报 `42501`（读也会返回空）** | **Supabase → SQL Editor 执行 `alter table units disable row level security;`**，再用第 2 节的验证脚本确认「写入成功」 |
| 页面提示「云端保存失败（数据仅存本地）：...row-level security...」 | 同上，新版前端会显式报错 | 执行上面的 `disable row level security`，强刷 |
| 顶栏「实时同步：未连接」 | 表未加入 Realtime 发布 | 执行 `alter publication supabase_realtime add table units;` |
| 别的设备改了，这边要手动刷新才看得到 | 实时未开启/未连上 | 见 4.1 节；确认徽标为绿色「已连接」 |

---

*最后更新：2026-09-07（补充 RLS 权限必做步骤与 42501 排错；修复导入数据未及时同步云端、静默失败不可见、云端返回 0 条误删本地数据）*
